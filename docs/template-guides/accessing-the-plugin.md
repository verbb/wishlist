# Accessing the Plugin

Most Twig tasks use the list, item and action helpers directly. Access the plugin instance only when a documented integration requires it; this is not necessary for ordinary wishlist rendering.

## Calls Used in This Task

### `craft.wishlist.getPlugin()`
Returns an instance of the Wishlist plugin.

